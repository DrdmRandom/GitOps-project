apiVersion: v1
kind: Service
metadata:
  name: linker-linker-direct
  namespace: linker
  labels:
    app.kubernetes.io/name: linker
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: linker
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30010
